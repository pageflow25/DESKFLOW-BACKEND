-- COMO CRIAR O AMBIENTE VIRTUAL, ENTRAR E BAIXAR AS BIBLIOTECAS
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt

-- COMO RODAR
uvicorn main:app --reload --host 0.0.0.0 --port 8000